# tinydb 操作手册

> 本手册面向 tinydb v0.20 用户（开发者、嵌入式集成者、SQL 学习者），覆盖安装、SQL 方言、ACID 使用方式、REPL 操作、并发控制、codec 异常合约、错误诊断与故障排查。
>
> **v0.20 累计变更**：v0.1.1 codec-exception-consistency + v0.2 JOIN/并发 + 2026-07-24 cli-enhancements (12 元命令) + 2026-07-27 review-fixes (9 项架构 hardening)。详细见 §11 v0.20 review-fixes hardening。

## 1. 安装

### 1.1 用户安装（仅运行时）

tinydb 运行时**零依赖**——完全使用 Python 3.11+ 标准库。从源码或 git 安装：

```bash
# 从 PyPI（一旦发布）
pip install tinydb

# 从 GitHub
pip install git+https://github.com/babooooooooooo/tinydb_cc_comet.git
```

安装后 Python 中可直接 `import tinydb`，命令行新增 `tinydb-repl` 命令。

### 1.2 开发者安装（带测试工具链）

```bash
git clone https://github.com/babooooooooooo/tinydb_cc_comet.git
cd tinydb_cc_comet
pip install -e ".[dev]"
```

`[dev]` 额外依赖：`pytest>=7`, `hypothesis>=6`, `pytest-cov>=4`。

### 1.3 系统要求

- Python ≥ 3.11
- 可选: readline（Unix 平台默认提供；用于 REPL 历史持久化）
- 操作系统: Linux / macOS / Windows（Windows REPL 无历史持久化）

## 2. 5 分钟快速上手

### 2.1 Python API

```python
import tinydb

# 内存数据库（重启即丢失）
with tinydb.Database(":memory:") as db:
    db.execute("CREATE TABLE users(id INT PRIMARY KEY, name TEXT, age INT)")
    db.execute("INSERT INTO users(id, name, age) VALUES (1, 'alice', 30)")
    db.execute("INSERT INTO users(id, name, age) VALUES (2, 'bob', 25)")
    for row in db.execute("SELECT * FROM users WHERE age >= 26"):
        print(row.id, row.name, row.age)

# 文件数据库（持久化）
with tinydb.Database("data.db") as db:
    db.execute("CREATE TABLE kv(k TEXT PRIMARY KEY, v INT)")
    db.execute("INSERT INTO kv(k, v) VALUES ('counter', 0)")
    for row in db.execute("SELECT * FROM kv"):
        print(row.k, row.v)
```

### 2.2 REPL

```bash
tinydb-repl                    # 内存数据库
tinydb-repl --database data.db # 文件数据库
```

交互样例：

```text
tinydb> CREATE TABLE accounts(id INT PRIMARY KEY, balance INT);
OK
tinydb> INSERT INTO accounts(id, balance) VALUES (1, 100);
OK
tinydb> INSERT INTO accounts(id, balance) VALUES (2, 50);
OK
tinydb> SELECT * FROM accounts WHERE balance >= 60;
id | balance
---+--------
 1 |    100
tinydb> .tables
accounts
tinydb> .schema accounts
CREATE TABLE accounts(id INT PRIMARY KEY, balance INT);
tinydb> .exit
```

## 3. SQL 方言速查

### 3.1 DDL

| 语句 | 形式 | 说明 |
|---|---|---|
| `CREATE TABLE` | `CREATE TABLE name(col1 TYPE, col2 TYPE(...), ...)` | 至少一列；列约束 `PRIMARY KEY` / `UNIQUE` / `NOT NULL` 可附加在列定义末尾 |
| `DROP TABLE` | `DROP TABLE name` | 删除表及其占用的页（释放空间） |

支持的 TYPE 见 [§ 4 类型系统](#4-类型系统)。

### 3.2 DML

| 语句 | 形式 | 说明 |
|---|---|---|
| `INSERT` | `INSERT INTO t(col1, col2, ...) VALUES (v1, v2, ...)` | 必须显式列出列；多行用逗号拼接 |
| `SELECT` | `SELECT [* \| col1, col2, ...] FROM t [WHERE cond] [ORDER BY col [ASC\|DESC]]` | 返回的每一行是不可变 `Row` 对象 |
| `UPDATE` | `UPDATE t SET col=val [, col=val ...] WHERE cond` | 受 WHERE 限制；无 WHERE 当前 MVP 不支持 |
| `DELETE` | `DELETE FROM t WHERE cond` | 必须有 WHERE |

### 3.3 WHERE 条件

tinydb 支持复合条件：

```sql
SELECT * FROM t
WHERE a = 1 AND b = 'x' OR NOT (c = 'y');
```

运算符：`=`, `<>`, `<`, `<=`, `>`, `>=`，用 `AND` / `OR` / `NOT` / 括号组合。

**重要**: `WHERE col = literal` 要求**精确同类型**——`INT ≠ SMALLINT ≠ BIGINT`，`FLOAT ≠ DOUBLE`，跨类型比较抛 `CodecError`（见 [§ 5 错误诊断](#5-错误诊断)）。

### 3.4 事务控制

每个 `db.execute()` 默认自动提交。如果需要多个语句作为一个原子单元：

```python
with tinydb.Database("data.db") as db:
    db.execute("BEGIN")
    try:
        db.execute("INSERT INTO accounts(id, balance) VALUES (2, 50)")
        db.execute("INSERT INTO accounts(id, balance) VALUES (3, 75)")
        db.execute("COMMIT")  # 一起生效
    except Exception:
        db.execute("ROLLBACK")
        raise
```

显式 `BEGIN` 后所有执行都加入当前事务，直到 `COMMIT` 或 `ROLLBACK`。如果中途抛异常，未显式 `COMMIT` 的更改也会自动回滚（autocommit 行为反过来——`BEGIN` 后的语句是手动提交）。

### 3.5 多表 JOIN（v0.2 新增）

tinydb v0.2 支持多表 `JOIN`：

| 关键字 | 含义 |
|---|---|
| `INNER JOIN` / `JOIN` | 仅返回两表能匹配上的行 |
| `LEFT JOIN` | 保留左表全部行，右表未匹配则 `NULL` 填充 |
| `RIGHT JOIN` | 保留右表全部行，左表未匹配则 `NULL` 填充 |
| `FULL JOIN` | 两边全部保留，未匹配侧 `NULL` 填充 |
| `CROSS JOIN` | 笛卡尔积（无需 `ON` / `USING`） |

连接条件：

| 形式 | 含义 |
|---|---|
| `ON expr` | 通用布尔表达式（支持 `AND` / `OR` / `NOT` 与现有比较运算符） |
| `USING (col, ...)` | 指定公共列名做等值连接，输出列合并（`Coalesce` 语义） |
| `NATURAL [INNER\|LEFT\|RIGHT\|FULL] JOIN` | 自动取两表公共列名做等值连接；无公共列时退化为 `CROSS JOIN` |

支持表别名与限定列引用：

```sql
SELECT u.id, o.id
FROM users AS u
JOIN orders o ON u.id = o.uid;
```

`SELECT *` 在多表上展开为限定列标签（如 `u.id` / `o.id`），避免同名列冲突。USING / NATURAL 合并键标签为非限定列名，可通过 `row["id"]` 取合并值。

完整示例（结合 GROUP BY + HAVING + ORDER BY + LIMIT）：

```sql
SELECT u.id, o.id, SUM(o.total) AS s
FROM users u
JOIN orders o ON u.id = o.uid
GROUP BY u.id
HAVING SUM(o.total) > 100
ORDER BY s DESC
LIMIT 10;
```

错误诊断：

| 错误 | 子类型 |
|---|---|
| 未知表 / 别名 | `UnknownSource` |
| 未知限定列 | `UnknownQualifiedColumn` |
| 重复别名 | `DuplicateAlias` |
| 歧义裸列 | `AmbiguousColumn` |
| USING 列缺失 | `MissingUsingKey` |
| USING/NATURAL 键类型不兼容 | `IncompatibleKeyTypes` |

所有 JOIN 名称解析错误都是 `tinydb.ResolutionError`（`ExecutionError` 的子类）。

**内存限制**：JOIN 路径采用 nested-loop + 物化宽行策略，无硬性行数上限。极大结果集（如百万级 × 百万级 CROSS JOIN）会消耗大量内存。建议在 application 层显式加 `LIMIT` 或在 `WHERE` 中加过滤条件（详见 `docs/MVP_LIMITATIONS.md` § v0.2 JOIN 内存限制）。

### 3.6 并发控制（v0.2 引入，v0.20 加固）

tinydb 默认提供**双层并发保护**：

1. **进程内**: `Database` 实例持有 `threading.RLock`（粗粒度、可重入），串行化 `execute()` 与 `explain_plan()`。
2. **跨进程**: `Pager` 在打开 DB 文件后立即获取 `fcntl.flock(LOCK_EX)`，第二个进程打开同一 DB 文件立即抛 `DatabaseLocked`。

```python
import tinydb
from tinydb import DatabaseLocked

# 默认：双层锁均启用
db = tinydb.Database("/path/to/file.db")

# 单线程 / 外部已有并发控制：显式 opt-out（零开销）
db = tinydb.Database("/path/to/file.db", locking=False)

# :memory: 模式：仅线程锁，无文件锁
db = tinydb.Database(":memory:")

# 跨进程争用
# 进程 A: db = tinydb.Database("/x.db")  →  持有 flock
# 进程 B: tinydb.Database("/x.db")        →  抛 DatabaseLocked("/x.db")
try:
    db = tinydb.Database("/x.db")
except DatabaseLocked as e:
    print(f"DB {e.path} is locked by another process")
```

**v0.20 hardening**（review-fixes T7）：`DatabaseLocked` 现继承 `tinydb.errors.ExecutionError`——`except ExecutionError` 能同时捕获跨进程争用和 SQL 执行错误，统一上层错误处理。

**Database 生命周期**（review-fixes T4 + T8）：

- `db.close()` 幂等，重复调用无副作用
- 关闭后调用 `execute()` / `explain_plan()` 抛 `RuntimeError("Database is closed")`
- 进程内所有 `execute()` 共享同一个 `RLock`——同一线程内 `BEGIN` → `INSERT` → `COMMIT` 这种串行调用天然不会自死锁

**平台限制**：

- **Linux / WSL2**: `fcntl.flock` 完整支持，默认行为即双层锁
- **Windows / 缺少 fcntl 的平台**: `locking=True` 在 `Pager.__init__` 立即抛 `ImportError("tinydb concurrency control requires fcntl (Linux/WSL only)")`。在该平台运行时显式传 `locking=False` 走单线程路径
- **macOS**: `fcntl` 模块存在但 `flock` 语义不可靠；建议在 macOS 上显式 `locking=False`，由应用层提供并发控制

**已知限制**：

- **读并发被牺牲**：无 MVCC / reader-writer split，所有 `execute()`（含 `SELECT`）走 EX 锁，长操作会阻塞其他线程
- **持锁时间含 fsync**：每次写提交 `fsync(main)` 在持锁期间执行；嵌入式 OLTP 单语句 fsync 通常 < 10 ms 可接受
- **`:memory:` 仅线程安全**：内存 dict 跨进程天然隔离，不获取文件锁

完整公开契约见 [`docs/superpowers/specs/concurrency-control.md`](../superpowers/specs/concurrency-control.md)。

## 4. 类型系统

15 个列类型，全部走 [codec registry](#codec-contract)，任何边界越界都抛 `CodecError`。

| SQL 类型 | 参数 | 字节布局 |
|---|---|---|
| `INT` / `INTEGER` | — | 4 字节有符号 |
| `SMALLINT` | — | 2 字节有符号（±32767） |
| `BIGINT` | — | 8 字节有符号 |
| `FLOAT` / `REAL` | — | 4 字节 IEEE 754 单精度（≈7 位精度） |
| `DOUBLE` | — | 8 字节 IEEE 754 双精度 |
| `TEXT` | — | 2 字节长度前缀 + UTF-8 |
| `VARCHAR(N)` | N ≥ 1 | 2 字节长度前缀 + UTF-8，最长 N 字节 |
| `CHAR(N)` | N ≥ 1 | 2 字节长度前缀 + UTF-8，写入右补空格至 N 字节（SQL92 PAD SPACE） |
| `BOOL` / `BOOLEAN` | — | 1 字节 |
| `DECIMAL(p, s)` | 1 ≤ p ≤ 18, 0 ≤ s < p | 8 字节 scaled int64（最高 18 位精度） |
| `DATE` | — | 4 字节 days since 1970-01-01 UTC |
| `TIME` | — | 4 字节 seconds since midnight UTC |
| `TIMESTAMP` | — | 8 字节 seconds since 1970-01-01 UTC |

**类型化字面量前缀**：

```sql
INSERT INTO events(d, t, ts, amt) VALUES (
  DATE '2026-07-16',
  TIME '14:30:00',
  TIMESTAMP '2026-07-16 14:30:00',
  DECIMAL '99.99'
);
```

## 5. 错误诊断

### 5.1 异常层次

```
TinydbError                   顶层
├── ParseError                解析阶段 (line/col 信息)
├── TokenError                词法阶段 (line/col 信息)
├── ExecutionError
│   ├── ConstraintViolation   NOT NULL / UNIQUE / PK 违规
│   ├── DatabaseLocked        跨进程文件锁争用（v0.20 起归于 ExecutionError）
│   └── ResolutionError       JOIN 名称解析（v0.2）
│       ├── UnknownSource              未知表/别名
│       ├── UnknownQualifiedColumn     未知限定列 (u.missing)
│       ├── DuplicateAlias             重复别名
│       ├── AmbiguousColumn            歧义裸列
│       ├── MissingUsingKey            USING 列在任一侧缺失
│       └── IncompatibleKeyTypes       USING/NATURAL 键类型不兼容
├── InvalidDatabaseFile       DB 文件结构损坏或字段格式不可识别
├── UnsupportedSchemaVersion  DB 版本过新，旧代码不认识
├── SchemaMismatch            DB 版本过旧，需要迁移
├── PageFull                  页容量耗尽
├── CatalogFull               catalog 页溢出 (> 4 KB)
└── CodecError (来自 tinydb.type_system) — type-level validation failure
        注：CodecError 多继承 TypeError/ValueError/OverflowError
```

所有 `TinydbError` 子类共享一个根异常，便于统一捕获。

### 5.2 Codec 异常合约（关键）

codec 校验失败时**只**抛 `tinydb.type_system.CodecError`。从 v0.1.1 起，**每个** codec 的 `encode_py` 都把 `self.validate(value)` 作为第一行执行——错误类型错误、值越界、长度溢出、精度溢出、`FLOAT`/`DOUBLE` 收到 `NaN`/`Infinity`，所有 type-level 失败都收敛到 `CodecError`。任何直接 `codec.encode_py(...)` 调用都不再泄漏 `AttributeError`/`TypeError`/`OverflowError`/`ValueError`。

| 触发条件 | 抛 `CodecError` 示例 |
|---|---|
| `INT` / `SMALLINT` / `BIGINT` 越界 | `CodecError: INT out of range: 2147483648` |
| `INT` 收到 `bool` (`True`/`False`) | `CodecError: expected int for INT, got bool` |
| `FLOAT` / `DOUBLE` 收到 `NaN` / `Infinity` | `CodecError: FLOAT inf/NaN not allowed: nan` |
| `VARCHAR(N)` 字符串长度 > N（按 UTF-8 字节） | `CodecError: VARCHAR(10) length 11 exceeds max` |
| `CHAR(N)` 同上（v0.1.1 之前会误报为 `VARCHAR(N)`） | `CodecError: CHAR(5) length 6 exceeds max` |
| `DECIMAL(p, s)` 数值超精度（v0.1.1 之前抛裸 `OverflowError`） | `CodecError: DECIMAL(10,2) value 1000.00 out of range` |
| `DECIMAL(p, s)` scaled 内部溢出 | `CodecError: DECIMAL(18,6) scaled value overflow` |
| `TIME` 越界（v0.1.1 之前抛 `ValueError`） | `CodecError: TIME out of range: 86400` |
| `WHERE col = literal` 跨类型 | `CodecError: type mismatch: ...` |
| 索引路径下 `WHERE id = 2147483648` on `INT PRIMARY KEY`（v0.1.1 之前静默 `[]`） | `CodecError: INT out of range: 2147483648` |

`CodecError` 是 type-level 错误的**唯一**异常源。**新代码应捕获 `CodecError`**，不要捕获 `TypeError`/`OverflowError`/`ValueError`。

> **向后兼容**：`CodecError` 多继承 `(TypeError, ValueError, OverflowError)`——已有 `except (TypeError, ValueError, OverflowError)` 的旧代码会自动捕获 `CodecError`。**注意**：v0.1.1 之前多继承是 load-bearing 的（部分 codec 抛父类），v0.1.1 之后所有 codec 都直接抛 `CodecError`，多继承仅作 backward-compat shim。

### 5.3 常见异常 → 处理建议

| 异常 | 含义 | 建议处理 |
|---|---|---|
| `ParseError` / `TokenError` | SQL 语法或词法错误 | 修正 SQL；`.line` / `.col` 字段定位出错位置 |
| `ConstraintViolation(kind='null')` | 写入 NULL 到非空列 | 检查 INSERT/UPDATE 是否提供了该列；或允许该列 NULL |
| `ConstraintViolation(kind='unique')` | 违反 UNIQUE 约束 | 查重；如合理则 `UPDATE` 而非 `INSERT` |
| `ConstraintViolation(kind='duplicate_pk')` | PK 重复 | 用 `WHERE id = ...` 先查；决定 `UPDATE` 还是抛错 |
| `DatabaseLocked` (v0.2) | 另一个进程持有了此 DB 文件的 `fcntl.flock` | 等待对端释放；或在 `Database(...)` 加 `locking=False` |
| `InvalidDatabaseFile` | 文件已损坏或字段格式不可识别（含 schema v1 旧 `[name, type]` 数组形态） | 若是 schema 升级：从 v1 文件迁移到 `Column.to_dict()` 形态 |
| `SchemaMismatch` | DB 文件版本高于当前代码支持的最高版本 | 升级 tinydb |
| `UnsupportedSchemaVersion` | 同上 | 同上 |
| `PageFull` | 单页耗尽 | 减小单行大小；`tinydb-engine-v2` 引入 overflow chain 自动扩展 |
| `CatalogFull` | catalog 页超过 4 KB | 删除冗余表；或将 catalog 多页化（roadmap） |
| `CodecError` | type-level 校验失败 | 见 [§ 5.2](#52-codec-异常合约关键) |
| `ResolutionError`（JOIN） | 名称解析失败（未知表 / 未知列 / 歧义列 / USING 缺失） | 修正 SQL 别名与列引用；见 [§ 3.5](#35-多表-joinv02-新增) |
| `RuntimeError("Database is closed")` (v0.20) | 在已 `close()` 的 `Database` 上调 `execute()` / `explain_plan()` | 重新 `Database(...)`；检查是否在 `with` 块外使用 |

### 5.4 异常处理范式

```python
import tinydb

try:
    db.execute("INSERT INTO t(i) VALUES (2**40)")
except tinydb.errors.CodecError as e:
    print(f"type-level error: {e}")
except tinydb.errors.ConstraintViolation as e:
    print(f"constraint failed: kind={e.kind}, column={e.column}")
except tinydb.errors.TinydbError as e:
    print(f"other tinydb error: {type(e).__name__}: {e}")
```

## 6. REPL 操作

### 6.1 启动

```bash
tinydb-repl                    # 内存数据库
tinydb-repl --database data.db # 打开/创建文件数据库
```

进入后看到 `tinydb>` 提示符。文件数据库的所有修改自动 `fsync`。

### 6.2 多行输入

未闭合的单引号 `'` 或未配对的 `(` 会进入续行模式 `...>`：

```text
tinydb> SELECT * FROM users
...> WHERE name = 'alice
...> ' OR age > 30;
```

只要 SQL 一致，多行粘贴是允许的（粘贴多行 SQL 即可）。

### 6.3 元命令（v0.20 共 12 个）

| 命令 | 说明 |
|---|---|
| `.help` | 列出所有元命令 + 键盘快捷键 |
| `.exit` / `.quit` | 退出 REPL（alias） |
| `.tables` | 列出所有表名（每行一个） |
| `.schema <name>` | 反向生成 `CREATE TABLE` 语句 |
| `.read <path>` | 读取并执行 UTF-8 SQL 脚本文件（v0.20 优化为 O(n) list-join） |
| `.explain <sql>` | 打印逻辑查询计划（不执行 SQL） |
| `.indexes [table]` | 列出所有索引，或仅指定表的索引 |
| `.stats` | 显示表数、行数、页数、空闲页数、WAL 大小 |
| `.timer on\|off` | 启用或关闭查询计时（启用时结果末尾追加 `Time: X.XXX ms`） |
| `.format table\|csv\|json` | 选择输出格式（默认 `table`：对齐 ASCII 列；`csv`：RFC 4180；`json`：array-of-objects） |
| `.color on\|off` | 启用或关闭交互高亮颜色 |

### 6.4 错误显示

执行错误显示为单行 `ERROR: <ExceptionClass>: <message>`，**不退出** session——直接重新输入 SQL 即可。

```text
tinydb> INSERT INTO t(i) VALUES (2147483648);
ERROR: CodecError: INT out of range: 2147483648
tinydb> SELECT * FROM t;
[results...]
```

### 6.5 历史记录

- Unix: `~/.tinydb_history`（自动加载/保存）
- Windows / 无 readline: 内置 `input()` 兜底——SQL、元命令、输出全工作，但历史不持久化

## 7. 性能特性与限制

### 7.1 适合的场景

- 嵌入式数据库（教学 / 工具 / 单进程服务）
- 中小数据集（单库 < 1 GB）
- 需要 ACID 而不想拖入 SQLite（更轻、更可读、易于 fork/扩展）
- 学习目的——逐行可读的存储引擎实现

### 7.2 不适合

- 高并发 OLTP（单 RLock + 跨进程 flock 会同时阻塞读、写；无 MVCC）
- 大数据量（无 page eviction，DB 常驻内存）
- 高 QPS 写入（autocommit 默认 `fsync`，受限于磁盘 `fsync` 速度）
- 跨平台写密集（fcntl 仅在 Linux/WSL2 可靠；macOS / Windows 上 `locking=True` 抛 `ImportError`，需 `locking=False`）

详见 [`MVP_LIMITATIONS.md`](MVP_LIMITATIONS.md)。

## 8. 故障排查

### 8.1 `ModuleNotFoundError: No module named 'tinydb'`

未安装 tinydb。重新运行 `pip install -e .` 或在脚本前加入 `sys.path.append(...)`。

### 8.2 `tinydb.errors.CodecError: INT out of range: <value>`

写入 INT 超出 4 字节有符号范围（−2³¹ .. 2³¹−1）。换 `BIGINT`。

### 8.3 `tinydb.errors.CodecError: VARCHAR(N) length M exceeds max`

UTF-8 字节长度超限。注意中文字符是 3 字节/字符。`CHAR` 同样规则。

### 8.4 `tinydb.errors.ConstraintViolation: kind='duplicate_pk'`

主键重复。先 `SELECT` 查询是否已存在。如需要替换，先 `DELETE` 再 `INSERT`，或用 `UPDATE`。

### 8.5 `tinydb.errors.InvalidDatabaseFile: unrecognized column entry: ['id', 'INT'] ...`

文件是 schema v1 格式——遗留 `["id", "INT"]` 数组形态。当前 tinydb 只读 v2+ 的 `Column.to_dict()` 对象形态。先迁移：

```python
# 一次性迁移：将 v1 catalog 导出为 Python 数据，导入到 v2 新数据库
import json
from tinydb import Database
from tinydb.catalog import Column

with open("legacy_data.json") as f:
    legacy = json.load(f)

with Database("new.db") as db:
    for tname, info in legacy["tables"].items():
        cols = [Column(name=n, type=ty, primary_key=(n == 'id')) for n, ty in info["schema"]]
        db.execute(f"CREATE TABLE {tname}(...)")  # 显式列定义
        for row in info["rows"]:
            # 逐行 INSERT ...
            ...
```

### 8.6 `tinydb.errors.SchemaMismatch: ...`

DB 文件版本高于当前代码识别范围。升级 tinydb：

```bash
pip install -U tinydb
```

### 8.7 REPL 没有历史

平台无 `readline` 模块（Windows 默认）。换 Unix 平台或安装 `pyreadline` / `gnureadline`。

### 8.8 测试运行报错 `pytest: command not found`

未安装 dev 依赖：

```bash
pip install -e ".[dev]"
```

## 9. 命令速查

```bash
# 安装
pip install -e ".[dev]"

# 运行 demo
python3 examples/demo.py

# 启动 REPL
tinydb-repl --database data.db

# 运行全部测试（993 tests + 2 skipped + 3 deselected via -m 'not slow'）
pytest -q

# 仅 perf benchmarks（5MB / 16MB .read + small correctness）
pytest -m slow -q

# 仅覆盖率
pytest --cov=src/tinydb

# 静态检查
pyflakes src/tinydb/

# 修复 coverage 警告
pytest --cov=src/tinydb --cov-report=term-missing
```

## 10. 进一步阅读

- [`MVP_LIMITATIONS.md`](MVP_LIMITATIONS.md) — MVP 不做什么
- [`README.md`](../README.md) — 项目顶层说明
- [`功能测试报告.md`](功能测试报告.md) — 测试覆盖与质量基线
- [`superpowers/reports/`](../superpowers/reports/) — 每次发布前的验证报告（最新：`2026-07-28-review-fixes-verify.md`）

## 11. v0.20 review-fixes hardening（架构级变更）

v0.20 是一次 review-driven hardening change，闭合 9 个 HIGH review findings（`tinydb-review-2026-07-28-fixes`，已 merged via `ab25451`）。模块级影响：

| # | 模块 | 修复要点 |
|---|------|---|
| 1 | `wal.py` | WAL write-ahead commit ordering：commit record 必须在 `fsync(main)` 之前落盘，避免 crash 时丢已提交事务 |
| 2 | `btree.py` | B+tree split 后立刻 patch `right.next_leaf_id` 指向新的最右 leaf，否则 range scan 跨 split 边界会漏行 |
| 3 | `catalog.py` | catalog overflow 真分裂 (greedy)，multi-page catalog 不再依赖 `_table_data_pages` workaround |
| 4 | `database.py` | Database 关闭是 race-safe 的 `_is_closed` 标志位 + `__init__` 失败路径上已部分构造资源的清理 |
| 5 | `_repl_meta.py` | REPL `.read` 大文件加载：`buf += char` (O(n²)) → list-append + `''.join()` (O(n))；新增 3 个 perf 基准 (`-m slow`) |
| 6 | `type_system.py` | codec round-trip 对称性 + `VARCHAR/CHAR` decode 边界严格化（不再静默接受超过声明长度的字节） |
| 7 | `database.py` | `DatabaseLocked` 继承链对齐：`DatabaseLocked(Exception)` → `DatabaseLocked(ExecutionError)`，让统一 `except ExecutionError` 能捕获跨进程争用 |
| 8 | `database.py` | `close()` 后 `execute()` / `explain_plan()` 抛 `RuntimeError("Database is closed")`；`close()` 自身幂等 |
| 9 | `type_system.py` | `codec_for(type_name, params)` 参数校验：参数缺失 / 类型错误直接抛 `CodecError`，不再返回 None 后被 caller 误用 |

每项修复都对应 RED → GREEN 测试 (`tests/unit/...` / `tests/integration/...`)。3 个 deviation 记录于 `tasks.md §9` + verify report：

- **DV-T5-1**: perf bounds 放宽 (5MB<1.5s / 16MB<6s) + `slow` marker
- **DV-T5-2**: perf test 用 monkey-patched `_run_sql` 隔离 buffer-build path
- **DV-T4-flake**: `close_during_execute` 在全负载下 1/996 transient flake

完整 RED → GREEN 证据见 [`docs/superpowers/reports/2026-07-28-review-fixes-verify.md`](../superpowers/reports/2026-07-28-review-fixes-verify.md)。
